import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  // Recharts 3 + es-toolkit breaks when Vite pre-bundles (require_isUnsafeProperty error)
  optimizeDeps: {
    exclude: ['recharts'],
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
      },
    },
  },
})
